print('Quel est le genre ? Noter: H ou F')
genre=str(input())
print("Quel est l'ethnie ? Entrer Blanc, Noir, Indien, Asie de l'Est, Asie du Sud-Est, Moyen-Orient ou Latino")
ethnie=str(input())

entetes=[u'photo', u'age', u'gender', u'ethnie']
valeurs=[]
#on obtient date_photo et data_naissance avant
valeurs.append([image_interet, str(age), genre, ethnie])

dataset=open('dataset.csv','a')

#si on veut entrer le titre des colonnes mettre ca lors de la 1ere execution puis le mettre en commentaire 
#ligneEntete = ";".join(entetes) + "\n" 
#dataset.write(ligneEntete)             

for valeur in valeurs:
     ligne = ";".join(valeur) + "\n"
     dataset.write(ligne)

dataset.close()
